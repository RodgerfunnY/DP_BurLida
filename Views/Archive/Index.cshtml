@model IEnumerable<DP_BurLida.Data.ModelsData.OrderModelData>

@{
    ViewData["Title"] = "Архив заказов";
}

<h1>Архив заказов</h1>
<p>Отображаются только завершенные заказы</p>

<div class="row mb-3">
    <div class="col-md-6">
        <a asp-controller="Order" asp-action="Index" class="btn btn-primary">Вернуться к заказам</a>
    </div>
    <div class="col-md-6">
        <form asp-action="Index" method="get" class="d-flex">
            <div class="input-group">
                <input type="text" name="searchTerm" class="form-control" 
                       placeholder="Поиск по адресу, насосу, обустройству, телефону, имени..." 
                       value="@ViewBag.SearchTerm"
                       title="Поиск по адресу, насосу, обустройству, телефону, имени, координатам и др." />
                <button type="submit" class="btn btn-outline-secondary">
                    Поиск
                </button>
                @if (!string.IsNullOrEmpty(ViewBag.SearchTerm as string))
                {
                    <a asp-action="Index" class="btn btn-outline-danger">
                        Очистить
                    </a>
                }
            </div>
        </form>
    </div>
</div>

@if (!string.IsNullOrEmpty(ViewBag.SearchTerm as string))
{
    <div class="alert alert-info">
        Результаты поиска по запросу: "<strong>@ViewBag.SearchTerm</strong>"
        @if (Model != null && Model.Any())
        {
            <span class="badge bg-primary ms-2">Найдено: @Model.Count()</span>
        }
        else
        {
            <span class="badge bg-warning ms-2">Ничего не найдено</span>
        }
    </div>
}

<div class="table-responsive">
<table class="table table-striped">
    <thead>
        <tr>
            <th>Адрес</th>
            <th>Скважина (диаметр)</th>
            <th>Насос</th>
            <th>Обустройство</th>
            <th>Телефон</th>
            <th>Имя клиента</th>
            <th>Дата завершения</th>
            <th>Действия</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @item.City
                </td>
                <td>
                    @item.Diameter
                </td>
                <td>
                    @{
                        var pumpText = item.Pump switch
                        {
                            0 => "не нужен",
                            600 => "монтаж насоса заказчика от 600 руб",
                            1100 => "насос от 1100 руб",
                            1101 => "насос от 1100 - взять 4куб насос",
                            _ => item.Pump.ToString()
                        };
                    }
                    @pumpText
                </td>
                <td>
                    @if (!string.IsNullOrEmpty(item.Arrangement))
                    {
                        @item.Arrangement
                    }
                    else
                    {
                        <span class="text-muted">Не указано</span>
                    }
                </td>
                <td>
                    @item.Phone
                </td>
                <td>
                    @item.NameClient
                </td>
                <td>
                    @item.CreationTimeData
                </td>
                <td>
                    <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-sm btn-info">Подробности</a>
                </td>
            </tr>
        }
    </tbody>
</table>
</div>

@if (!Model.Any())
{
    <div class="alert alert-warning">
        <h4>Архив пуст</h4>
        <p>В архиве нет завершенных заказов.</p>
    </div>
}
